import { render } from '@testing-library/react-native';
import GoalInput from '../GoalInput';

test('test', () => {
    // eslint-disable-next-line @typescript-eslint/no-empty-function
    render(<GoalInput setShowGoalInput={() => {}} />);
});
